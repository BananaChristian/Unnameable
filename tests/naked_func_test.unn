naked func raw_get_code() :i32 {
  asm{
      mov eax, 42;    # Load 42 directly into the return register
      ret             # Manually return to caller
  }
}

func main:i32{
  auto val=raw_get_code()
  return 0
}
