module test

func cleanup(ptr opaque whatever): void {}

component Player {
    i32 health
    fn(ptr opaque ):void drop
}

func main: i32 {
    Player p
    p.health = 100
    p.drop = cleanup
    p.drop()
    return 0
}
